#include<iostream>
#include<algorithm>
#include<string>

using namespace std;

int main()
{
    // string str;
    // cin>>str;
    // string str(5,'a');
    // string str = "amankeshri";
    // getline(cin,str); //it includes all stence with spaces
    // cout<<str;

    // string s1 = "fam";
    // string s2 = "ily";
    // s2.append(s1);
    // s1 = s1+s2;
    // cout<<s1+s2;

    string s1 = "ilydfgdtrtyh";
    string s2 = "ily";
    // cout<<s2.compare(s1);
    // s1.isempty()
    // s1.clear();
    // s1.erase(3,3); (index,length) ;
    // s1.find(subarray);
    // s1.insert(index, substring);
    // s1.size();
    // s1.length();
    // s = s1.substr(start index, no. of charcters)
    // int x = stoi(s1);  //string to integer
    // s = to_string(integer) // int to string
    // sort(s1.begin(),s1.end()); // sort(start index, end index)
    // cout<<s1;

    // convert to upper and lower case (inbuilt function)
    // transform(s1.begin(),s1.end(),s1.begin(), ::toupper);//transform(string start,string end, transform start index);
    // cout<<s1<<endl;
    // transform(s1.begin(),s1.end(),s1.begin(), ::tolower);
    // cout<<s1;

    // convert to upper and lower case 
    // for(int i=0;i<s1.size();i++)
    //     s1[i] -= 32;
    // cout<<s1<<endl;
    // for(int i=0;i<s1.size();i++)
    //     s1[i] += 32;
    // cout<<s1<<endl;

    // biggest number using a string of number
    // string s3 = "23848966969865";
    // sort(s3.begin(),s3.end()); // sort in ascending
    // cout<<s3<<endl;
    // sort(s3.begin(),s3.end(),greater<int>()); //sort in descending
    // cout<<s3;

    // find the maximum frequency of a character
    // int a[26];
    // for(int i=0;i<26;i++)
    //     a[i] = 0;
    
    // for(int i=0;i<s1.size();i++)
    // {
    //     a[s1[i]-'a']++;
    // }
    // int max = 0;
    // char ans;
    // for(int i=0;i<26;i++)
    // {
    //     if(a[i] > max)
    //     {
    //         max = a[i];
    //         ans = 'a'+i;
    //     }
    // }
    // cout<<max<<" "<<ans;


    // return 0;
}